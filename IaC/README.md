# Infrastructure as Code (IaC) for AI-Driven Personal Finance Assistant

This folder contains scripts and configurations to provision the AWS infrastructure required for the application.

## Prerequisites

- **AWS CLI:** Ensure that the AWS CLI is installed and configured.
- **Terraform or AWS CDK:** Depending on your chosen approach.
- **IAM Permissions:** Your AWS user must have the necessary permissions to create and manage resources.

## Setup Instructions

### Using Terraform

1. **Initialize Terraform:**
   ```bash
   terraform init
